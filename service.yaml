apiVersion: v1
kind: Service
metadata:
  name: rbac-visualizer
  namespace: default

spec:
  selector:
    app: rbac-visualizer

  ports:
  - port: 80
    targetPort: 8000

  type: ClusterIP